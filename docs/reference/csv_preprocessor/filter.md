# フィルター

## フィルターの作り方

1. src/filter にディレクトリを作成。
    - ディレクトリ名に'-'(ハイフンは使えない)。コマンド実行時のフィルタ名の'-'は'_')に変換するので、ディレクトリは'_')で作成する。
2. filter.py を作成
3. sampleフィルターを参考に実装

## filter_execute_table()の実装

table 引数の Table オブジェクトを加工する。カラムの追加や削除。行の追加や削除ができる。  
